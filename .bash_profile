# Server_Termux_All - shortcut
ALL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
alias menu="bash $ALL_DIR/menu.sh"
alias start_ninja="bash $ALL_DIR/ninja_start.sh"
alias start_htth="bash $ALL_DIR/htth_start.sh"
alias stop_all="bash $ALL_DIR/stop.sh"
